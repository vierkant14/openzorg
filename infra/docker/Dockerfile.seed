FROM node:20-alpine

# Pre-install tools so they don't need to be installed on every run
RUN apk add --no-cache curl postgresql-client

WORKDIR /seed
COPY infra/scripts/seed.sh /seed/seed.sh

CMD ["sh", "/seed/seed.sh"]
